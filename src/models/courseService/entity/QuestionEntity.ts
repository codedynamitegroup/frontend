export interface QuestionEntity {
  questionId: string;
  organizationId: string;
  difficulty: string;
  name: string;
  questionText: string;
  generalFeedback: string;
  defaultMark: number;
  message: string;
}
